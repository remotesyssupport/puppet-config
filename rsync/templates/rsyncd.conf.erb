uid = nobody
gid = nobody
use chroot = yes
max connections = 4
syslog facility = local5
pid file = /var/run/rsyncd.pid

[ftp]
        path = <%= vsftpd::ftpd_directory %>/./pub
        comment = ftp public area
